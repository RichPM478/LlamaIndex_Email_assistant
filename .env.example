# ============================================
# LLM Provider Configuration
# ============================================

# Choose your LLM provider: ollama | openai | azure_openai | anthropic
LLM_PROVIDER=ollama

# ---------- Ollama (Local) ----------
# Default: runs entirely on your machine, no external API calls
OLLAMA_MODEL=llama3.2:3b
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_NUM_CTX=2048

# ---------- OpenAI ----------
# Uncomment and add your key to use OpenAI
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# ---------- Azure OpenAI ----------
# For enterprise deployments
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name
# AZURE_OPENAI_API_VERSION=2024-06-01

# ---------- Anthropic ----------
# For Claude models
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-haiku-20240307

# ============================================
# Embeddings Configuration
# ============================================

# Choose embeddings: local_hf | openai
EMBEDDINGS_PROVIDER=local_hf

# Local Hugging Face models (runs on your machine)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# OpenAI embeddings (requires OPENAI_API_KEY)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ============================================
# Email Configuration (IMAP)
# ============================================

# Your email server settings
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_SSL=true
IMAP_USER=your.email@example.com
IMAP_PASSWORD=your-app-specific-password
IMAP_FOLDER=INBOX

# Optional filters (comma-separated or JSON array)
# Only process emails from specific senders
# FILTER_FROM=sender1@example.com,sender2@example.com
# FILTER_FROM=["sender1@example.com", "sender2@example.com"]

# Only process emails with specific subjects
# FILTER_SUBJECT=Meeting,Important,Action Required
# FILTER_SUBJECT=["Meeting", "Important", "Action Required"]

# ============================================
# Application Settings
# ============================================

# Timezone for date/time handling
APP_TIMEZONE=Europe/London

# Week start day for weekly summaries
WEEK_START=Monday

# ============================================
# Advanced Settings (Optional)
# ============================================

# Logging level: DEBUG | INFO | WARNING | ERROR
# LOG_LEVEL=INFO

# Maximum emails to process per batch
# MAX_BATCH_SIZE=500

# Vector index chunk size
# CHUNK_SIZE=800

# Chunk overlap for better context
# CHUNK_OVERLAP=100

# Number of results to return in searches
# SEARCH_TOP_K=6
